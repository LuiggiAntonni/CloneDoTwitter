mixin createPostForm(userLoggedIn)
    .postFormContainer 
        .userImageContainer
            img(src=userLoggedIn.profilePic, alt="User's profile picture")
        .textareaContainer 
            textarea#postTextarea(placeholder="Digite aqui suas militâncias...")
            .buttonsContainer
                button#submitPostButton(disabled="") Post 
