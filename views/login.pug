extends layouts/login-layout.pug

block content
    .loginContainer 
        h1 Login
        form(method="post") 
            input(type="text", name="logUsername", placeholder="Usuário ou email", required)
            input(type="password", name="logPassword", placeholder="Senha", required)
            input(type="submit", value="Login")
        a(href="/register") Não tem uma conta? Inscreva-se