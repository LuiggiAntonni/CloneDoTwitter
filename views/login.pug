extends layouts/login-layout.pug

block content
    .loginContainer 
        h1 Login
        form(method="post") 
            p.errorMessage #{errorMessage}
            .input-field
                i.fas.fa-user
                input.login(type="text", name="logUsername", placeholder="Usuário ou email", value=logUsername, required)
            .input-field
                i.fas.fa-lock
                input.login(type="password", name="logPassword", placeholder="Senha", required)

            input(type="submit", value="Login")
        a(href="/register") Não tem uma conta? Inscreva-se